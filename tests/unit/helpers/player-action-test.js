import { playerAction } from 'code-camp-js/helpers/player-action';
import { module, test } from 'qunit';

module('Unit | Helper | player action');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = playerAction([42]);
  assert.ok(result);
});
